import { buttons } from "constants/details/buttons";
import { ComponentDetailsList } from "types/types.d";

// TODO: any type
export const componentDetails: ComponentDetailsList | any = {
  // assets,
  // branding,
  buttons,
  // formElements,
  // iconButtons,
  // identicons,
  // infoBlocks,
  // loaders,
  // modals,
  // textLinks,
  // typography,
};
